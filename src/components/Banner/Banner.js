import React from 'react';
import './Banner.css';

const Banner = () => {
    return (
        <section className="banner">
            <h1>Make a team of professionals.</h1>
            <p>Turn your amazing ideas into live projects faster than before.</p>
        </section>
    );
};

export default Banner;